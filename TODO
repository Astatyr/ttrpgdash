# Open features

## Map ruler
A distance-measurement tool shown on the map in the DM window, player view, and replay window.
- DM activates via a toggle in the Map menu (or a hotkey)
- Click-drag on the map draws a ruler line between two points
- Shows the real-world distance in feet based on the map's width-in-feet setting
- Ruler is a visual overlay only — not persisted, not logged
- Visible on the player view so players can see the measurement
- Replay window supports the ruler as an interactive overlay — the viewer can draw and
  move it independently while stepping through the replay, same as the DM window

## Audio device selection
Dropdown at the top of the music bar showing all connected audio output devices.
- Enumerate with javax.sound.sampled.AudioSystem.getMixerInfo() (no extra library needed)
- Requires replacing JavaFX MediaPlayer with javax.sound.sampled for playback
  (MediaPlayer has no device-target API)
- MusicController owns all playback logic — swap is self-contained
- Must be memory-efficient: dispose/re-create Clip or SourceDataLine per device change,
  not per track play

## Named log sessions
Low priority. When enabling logging, optionally prompt for a human-readable session name
(e.g. "Session 5 — Dragon Cave") to use as the session folder name instead of the raw timestamp.
